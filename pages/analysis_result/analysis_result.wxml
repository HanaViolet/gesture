<view class="page-full result-page">
  <!-- 进度条 -->
  <view class="progress-wrapper">
    <progress percent="{{progress}}" stroke-width="8" activeColor="#007AFF" backgroundColor="#E3F2FD" show-info="{{false}}"/>
    <text class="progress-text" wx:if="{{progress < 100}}">翻译中 {{progress}}%</text>
    <text class="progress-text done" wx:else>翻译完成</text>
  </view>

  <!-- 主卡片 -->
  <view class="result-card card-shadow">
    <view class="result-hint">{{convertingText}}</view>
    <textarea class="result-textarea" placeholder="翻译结果将显示在这里，您可以编辑..." value="{{chatTexts}}" bindinput="onChatTextInput" maxlength="500"/>

    <!-- 语音选择 -->
    <view class="voice-actions">
      <view class="voice-btn male {{!maleAudioUrl ? 'disabled' : (selectedVoice === 'male' ? 'active' : '')}}" bindtap="playMaleVoice">
        <image src="/images/male_icon.svg" mode="aspectFit" class="voice-icon-img"/>
        <text>男声</text>
      </view>
      <view class="voice-btn female {{!femaleAudioUrl ? 'disabled' : (selectedVoice === 'female' ? 'active' : '')}}" bindtap="playFemaleVoice">
        <image src="/images/female_icon.svg" mode="aspectFit" class="voice-icon-img"/>
        <text>女声</text>
      </view>
    </view>
  </view>

  <!-- 底部按钮 -->
  <view class="result-footer">
    <view class="footer-btn btn-secondary press" bindtap="saveToCommonPhrases">
      <text>保存至常用语</text>
    </view>
    <view class="footer-btn btn-primary press" bindtap="confirmEdit">
      <text>生成语音</text>
    </view>
  </view>
</view>
